    # Get parameters

    my $inf = $_ZERO_REPLACE_{IN};

    # Do the replacement...

    my ($retval,$errmsg);
    $retval = cir_imrepl($inf,0.0,1.0,$errmsg);
    if ($retval != CIR_OK) {
	orac_throw("CIR_IMREPL: failed in _ZERO_REPLACE_\n$errmsg\n");
    }
