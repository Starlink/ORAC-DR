    # Get parameters

    my $inf = $_ZERO_REPLACE_{IN};

    # Do the replacement...

    my ($retval,$errmsg);
    $errmsg = "";
    $retval = cir_imrepl($inf,0.0,1.0,$errmsg);
    if ($retval != CIR_OK) {
	orac_err("CIR_IMREPL: failed in _ZERO_REPLACE_\n$errmsg\n");
	return(ORAC__ERROR);
    }
