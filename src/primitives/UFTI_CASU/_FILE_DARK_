    my $fname = $_FILE_DARK_{NAME};
    my $tmpobj = $Frm->new($fname);
    $Cal->dark($fname);
    orac_print $tmpobj->file . " filed as the current dark\n";
    $Cal->darkindex->add($tmpobj->file,$tmpobj->hdr);
    orac_print $tmpobj->file . " added to index file\n\n";
