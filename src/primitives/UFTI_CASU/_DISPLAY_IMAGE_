    # Loop for each image extension

    foreach my $i (1 .. $Frm->findnsubs) {
        my $subfr = $Frm->getasubframe($i);
        $Display->display_data($Frm,{WINDOW=>$i-1}) if (defined $Display);
    }
