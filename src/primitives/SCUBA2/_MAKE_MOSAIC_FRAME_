
# write alignment text file
my $intmp = new ORAC::TempFile;
for my $i (1..$Frm->nsubs) {
  print {$intmp->handle} $Frm->file($i) ."\n";
}

my ($in, $out) = $Frm->inout('_mos');

$Mon{ccdpack_reg}->obeyw("makemos","in=^".$intmp->file." nousevar scale out=$out zero");

$Frm->files( $out );
