
my $qa_log_entries = $Frm->uhdr( "QA_LOG_ENTRIES" );

foreach my $survey ( sort keys %{$qa_log_entries} ) {
  my $logfile = new ORAC::LogFile( "log.qa" );
  $logfile->addentry( @{$qa_log_entries->{$survey}} );
  $logfile->addentry( "" );
}

