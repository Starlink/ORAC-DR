
my $file = $_DISPLAY_FILE_{FILE};

if( defined( $file ) ) {

  # Create a new Frame object from the given file.
  my $newFrm = $Frm->new( $file );

  # Display it.
  $Display->display_data( $newFrm ) if ( defined( $Display ) );

}
