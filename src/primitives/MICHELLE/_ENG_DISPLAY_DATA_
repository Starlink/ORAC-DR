# ENG DISPLAY DATA -*-perl-*-
#


if (defined $Display) {
  my $prompt = "y";
  while ($prompt =~ /^y/i) {
    $Display->display_data($Frm) ;
    print "Would you like to redisplay this screen?>";
    chomp($prompt = <>);
  }
};
