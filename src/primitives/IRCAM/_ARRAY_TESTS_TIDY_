# _ARRAY_TESTS_TIDY_      -*- perl -*-
#
# Removes unwanted intermediate files for the ARRAY_TESTS recipe.
    
# Removed intermediate files stored $Frm->file method.  This is
# IRCAM specific.
    if ( $Grp->num == 7 ) {
       _DELETE_TEMP_GROUP_FILES_ DELETE=_cl,_nl,_nul
    }
