    # Look at the level that the sky subtraction is supposed to happen
    # at and act accordingly...

    if ($Grp->uhdr("SKYSUBLEVEL") eq "USTEP") {
        _USTEP_SKYSUB_
    } else {
        _JITTER_SKYSUB_INCL_
    }
