    # Do the quadrant background correction

    my ($errmsg,$retval);
    my $fname = $Frm->file;
    orac_print("Doing quadrant background correction on $fname\n");
    foreach my $i (1 .. $Frm->findnsubs) {
	my $frm = $Frm->getasubframe($i);
	$retval = cir_quadback($frm->file,$errmsg);
	if ($retval != CIR_OK) {
	    orac_err("CIR_QUADBACK: failed in _QUAD_BACKGROUND_\n$errmsg\n");
        }
    }

    
  
