    # Do the sky combination

    my $outf = sprintf("sky_%s_%05d%s",$Frm->hdr("ORACUT"),$Grp->name,
                      $Frm->fitssuffix);
    _SKYCOMBINE_ OUT=$outf

    # Now file the output into an index

    _FILE_SKY_ NAME=$outf


    
