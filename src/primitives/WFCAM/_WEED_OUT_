    my $minflux = (defined $_WEED_OUT_{MIN} ? $_WEED_OUT_{MIN} : 0);
    my $maxflux = (defined $_WEED_OUT_{MAX} ? $_WEED_OUT_{MAX} : 65535);
   
    # Do stats on input frame and see what the value for the first
    # image extension is.  If it's not in the required data window, then
    # chuck it.

    _IMSTAT_ HDRUP=1
    my $value = $Frm->getasubframe(1)->uhdr("CIRMED");
    if ($value < $minflux || $value > $maxflux) {
        $Frm->isgood(0);
    }

=head1 NAME

_WEED_OUT_ -- Remove frames from a group if they are over or under exposed.

=head1 DESCRIPTION

The background median for the current frame is calculated. If the background 
value falls outside of the lower and upper cuts defined in the input arguments
then the frame is flagged as a bad exposure.

=head1 ARGUMENTS

=over 4

=item MIN = float (Given)

The minimum background value the frame can have.

=back

=over 4

=item MAX = float (Given)

The maximum background value the frame can have.

=back

=head1 NOTES

None

=head1 AUTHORS

JRL: Jim Lewis (CASU, IoA)

=head1 COPYRIGHT

Copyright (C) 2005-2008 Cambridge Astronomy Survey Unit.
All Rights Reserved

=cut
