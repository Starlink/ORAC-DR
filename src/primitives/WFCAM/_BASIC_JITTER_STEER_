    # If this is part of a group that includes microstepping, then we 
    # do the sky subtraction at the microstep sequence level. This allows
    # us to do the interleaving before we have officially completed the
    # jitter sequence.

    if ($Frm->hdr("NUSTEP") != 1) {
        $Grp->uhdr("SKYSUBLEVEL","USTEP");
        $Grp->uhdr("ALLOWED_TO_INTERLEAVE",$Grp->uhdr("USTEP_COMPLETE"));
        $Grp->uhdr("ALLOWED_TO_DITHER",$Grp->uhdr("JITTER_COMPLETE"));
    } else {
        $Grp->uhdr("SKYSUBLEVEL","JITTER");
        $Grp->uhdr("ALLOWED_TO_INTERLEAVE",$Grp->uhdr("JITTER_COMPLETE"));
        $Grp->uhdr("ALLOWED_TO_DITHER",$Grp->uhdr("JITTER_COMPLETE"));
    }



