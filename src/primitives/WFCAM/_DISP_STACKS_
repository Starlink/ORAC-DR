    # Start by looking for any existing semaphore files

    opendir DD,$ENV{'ORAC_DATA_OUT'};
    my @allf = grep /\.disp_/,readdir DD;
    closedir DD;

    # Loop for all of them

    foreach my $sem (@allf) {
        if ($Display) {
            (my $infile = $sem) =~ s/\.disp_//;
            my $frm = $Frm->new($infile);
            {
                my $Frm = $frm;
                _DISPLAY_IMAGE_
            }
        }
        unlink $sem;
    }

=head1 DESCRIPTION

All the semaphores beginning with .disp_ are located in the ORAC_DATA_OUT
directory. If displaying has been requested then the files attatched to these
semaphores are displayed. If not then the semaphore is simply deleted.

=head1 ARGUMENTS

None

=head1 NOTES

None

=head1 AUTHORS

JRL: Jim Lewis (CASU, IoA)

=head1 COPYRIGHT

Copyright (C) 2005-2008 Cambridge Astronomy Survey Unit.
All Rights Reserved

=cut
