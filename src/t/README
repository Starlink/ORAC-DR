This directory contains some tests for the ORAC-DR core perl code.

This test suite is woefully inadequate.

Negative results indicate bugs.
Positive results indicate that we are not testing enough.

At the very least it makes sure that all the ORAC-DR modules load
and compile without warnings.
