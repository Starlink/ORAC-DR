_IMAGING_HELLO_
_ARRAY_TESTS_STEER_

# The following condition needs to be set up so that we don't
# have array test-specific checks in _FIND_BAD_PIXELS_
if($Frm->uhdr("ARRAY_TESTS_FIND_BAD_PIXELS")) {
  _FIND_BAD_PIXELS_
}

_MASK_BAD_PIXELS_

_BIAS_GROUP_
_MEASURE_READNOISE_
_MEASURE_DARK_CURRENT_