
# ORAC-DR environment login

# Must run xoracdr or oracdr_<inst> to do the full initialisation

# In order to run ORAC-DR you will also need to declare
#  ORAC_DIR  - the root location of the ORAC-DR tree
#   e.g /jac_sw/oracdr

#  ORAC_CAL_ROOT - the root of the calibration files
#   e.g. /jac_sw/oracdr_cal

setenv ORAC_PERL5LIB ${ORAC_DIR}/lib/perl5/
