# The dome flat field is filter dependent, so the filters must match.
FILTER eq $Hdr{FILTER}

# Write the oractime in the index to select the dome flat closest
# temporally to the current frame.
ORACTIME
