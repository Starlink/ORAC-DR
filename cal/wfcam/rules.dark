# Write the oractime in the index to select the dark closest
# temporally to the current frame.
ORACTIME

# Exposure time is pretty important for darks
EXP_TIME == $Hdr{EXP_TIME}

# as the readmode 
READMODE eq $Hdr{READMODE}

# and the number of coadds.
NEXP == $Hdr{NEXP}
