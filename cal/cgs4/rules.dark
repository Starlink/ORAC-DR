# Observation type must be 'DARK'
OBSTYPE eq 'DARK'

# Exposure time should match
DEXPTIME ==$Hdr{DEXPTIME}

# Array mode should match
# Not sure if this is stricly necessary (PH)
MODE eq $Hdr{MODE}

# Readout areas should match
RDOUT_X1 == $Hdr{RDOUT_X1}
RDOUT_X2 == $Hdr{RDOUT_X2}
RDOUT_Y1 == $Hdr{RDOUT_Y1}
RDOUT_Y2 == $Hdr{RDOUT_Y2}

ORACTIME
