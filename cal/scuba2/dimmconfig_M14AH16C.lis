# Custom config for M14AH16C: use bright_extended as a base and add
# the fix_blobs override. Doesn't entirely eliminate the blobs, but it
# does help.

^$STARLINK_DIR/share/smurf/dimmconfig_bright_extended.lis

^$STARLINK_DIR/share/smurf/dimmconfig_fix_blobs.lis
