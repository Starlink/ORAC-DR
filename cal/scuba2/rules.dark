# Write the oractime in the index to select the dark closest
# temporally to the current frame.
ORACTIME

# Exposure time is pretty important for darks
#EXP_TIME == $Hdr{EXP_TIME}

# SCUBA-2 DARKs are used to interpolate the dark level during an
# observation
OBSNUM == $Hdr{OBSNUM}

# SCUBA-2 DARKs need to know the subarray
SUBARRAY eq $Hdr{SUBARRAY}
