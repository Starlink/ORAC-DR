^$STARLINK_DIR/share/smurf/dimmconfig_blank_field.lis

# Makemap config file for SASSy Legacy Survey (MJLSY01)
# Use blank field config as a base, as SASSy is not concerned with
# extended structures.

# Remove restriction on map-making duration (the blank field config
# makes a map for every 4 minutes of data)
maxlen = 0
