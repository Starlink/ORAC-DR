FILTER ; ($Hdr{OBSTYPE} eq 'ARC' ? 1 : $Hdr{FILTER} eq 'FILTER')
CNFINDEX eq $Hdr{CNFINDEX}
SAMPLING ; 'SAMPLING' eq '1x1' || 'SAMPLING' eq $Hdr{SAMPLING}
ORACTIME
WAVEFORM eq $Hdr{WAVEFORM}
EXP_TIME eq $Hdr{EXP_TIME}